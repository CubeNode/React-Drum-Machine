/*import React from 'react';

function Pad(props) {
  return(
    <div className="drum-pad" id={props.id} onClick={() => {
      //this.handleClick
      //playSound(props.audioId, props.id);
      console.log(Event.target)
                                                         }}>
      <audio src={props.clip} className="clip" id={props.audioId}></audio>
      <p>{props.name}</p>
    </div>
  )
}

export default Pad*/
